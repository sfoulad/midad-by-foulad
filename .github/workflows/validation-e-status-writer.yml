name: "Validation E (disposable -- status-writer attempt)"

on:
  pull_request_target:
    types:
      - opened

permissions:
  statuses: write

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No-op
        run: echo "this workflow deliberately grants statuses: write outside thin-fork-guard-trusted.yml"
