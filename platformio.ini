[platformio]
default_envs = default
build_cache_dir = .cache
extra_configs = platformio.local.ini

[crosspoint]
version = 1.8.57

[base]
platform = https://github.com/pioarduino/platform-espressif32/releases/download/55.03.37/platform-espressif32.zip
board = esp32-c3-devkitm-1
framework = arduino
monitor_speed = 115200
upload_speed = 921600
check_tool = cppcheck
; useStlAlgorithm is suppressed deliberately, not out of convenience: it advises
; replacing raw loops with std::transform/accumulate/any_of, and every such
; substitution instantiates another function template. On a 380KB-RAM ESP32-C3
; with no PSRAM that trades flash and DRAM for no runtime benefit, which is the
; opposite of what CLAUDE.md's "Template and std::function Bloat" rule asks for.
; The advice is sound on a desktop target and wrong here.
check_flags = --enable=all --suppress=missingIncludeSystem --suppress=unusedFunction --suppress=unmatchedSuppression --suppress=useStlAlgorithm --suppress=*:*/.pio/* --inline-suppr
check_skip_packages = yes

board_upload.flash_size = 16MB
board_upload.maximum_size = 16777216
board_upload.offset_address = 0x10000

build_flags =
  -DARDUINO_USB_MODE=1
  -DARDUINO_USB_CDC_ON_BOOT=1
  -DEINK_DISPLAY_SINGLE_BUFFER_MODE=1
  -DDISABLE_FS_H_WARNING=1
  -DDESTRUCTOR_CLOSES_FILE=1
# https://libexpat.github.io/doc/api/latest/#XML_GE
  -DXML_GE=0
  -DXML_CONTEXT_BYTES=1024
  -std=gnu++2a
# Enable UTF-8 long file names in SdFat
  -DUSE_UTF8_LONG_NAMES=1
# Increase PNG scanline buffer to support up to 2048px wide images
# Default is (320*4+1)*2=2562, we need more for larger images
  -DPNG_MAX_BUFFERED_PIXELS=16416
  -Wno-bidi-chars
  -Wl,--wrap=panic_print_backtrace,--wrap=panic_abort,--wrap=bootloader_common_check_efuse_blk_validity
  -fno-exceptions

build_unflags =
  -std=gnu++11
  -fexceptions

; Board configuration
board_build.flash_mode = dio
board_build.flash_size = 16MB
board_build.partitions = partitions.csv
; Firmware-embedded Quran EPUB (tools/quran/build_quran_epub.py) -- see src/QuranBook.h
board_build.embed_files = data/quran.epub

extra_scripts =
  pre:scripts/build_html.py
  pre:scripts/gen_i18n.py
  pre:scripts/git_branch.py
  pre:scripts/patch_jpegdec.py
  post:scripts/register_unit_tests_target.py

; Libraries
lib_deps =
  BatteryMonitor=symlink://freeink-sdk/libs/hardware/BatteryMonitor
  InputManager=symlink://freeink-sdk/libs/hardware/InputManager
  EInkDisplay=symlink://freeink-sdk/libs/display/FreeInkDisplay
  SDCardManager=symlink://freeink-sdk/libs/hardware/SDCardManager
  BoardConfig=symlink://freeink-sdk/libs/hardware/BoardConfig
  PowerManager=symlink://freeink-sdk/libs/hardware/PowerManager
  FreeInkUI=symlink://freeink-sdk/libs/ui/FreeInkUI
  Icons=symlink://freeink-sdk/libs/assets/Icons
  bblanchon/ArduinoJson @ 7.4.2
  ricmoo/QRCode @ 0.0.1
  bitbank2/PNGdec @ 1.1.6
  https://github.com/bitbank2/JPEGDEC.git#86282979224c8a32fd51e091ed5a35b0c699a52b
  links2004/WebSockets @ 2.7.3

; Xteink X3 + X4 -- one ESP32-C3 binary, runtime-selected (HalGPIO::deviceIsX3()).
; Kept out of [base] (rather than the C3 device flags living there) so the
; ESP32-S3 envs below (sticky, x4pro) can extend [base] without inheriting
; C3-only device flags that don't apply to them.
[c3_xteink]
build_flags =
  -DFREEINK_DEVICE_X4=1
  -DFREEINK_DEVICE_X3=1

[env:default]
extends = base
build_flags =
  ${base.build_flags}
  ${c3_xteink.build_flags}
  ; CROSSPOINT_VERSION is set by scripts/git_branch.py (includes branch + short SHA)
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=2 ; Set log level to debug for development builds


[env:gh_release]
extends = base
build_flags =
  ${base.build_flags}
  ${c3_xteink.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}\"
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=1 ; Set log level to info for release builds

[env:gh_release_rc]
extends = base
build_flags =
  ${base.build_flags}
  ${c3_xteink.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}-rc+${sysenv.CROSSPOINT_RC_HASH}\"
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=1 ; Set log level to info for release candidate builds

[env:slim]
extends = base
build_flags =
  ${base.build_flags}
  ${c3_xteink.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}-slim\"
  ; serial output is disabled in slim builds to save space
  -UENABLE_SERIAL_LOG

; ============================================================================
; ESP32-S3 targets -- FOUNDATION ONLY, not yet feature-complete or hardware-
; verified. These extend [base] (compiler/toolchain/CrossPoint build flags) but
; are a genuinely different chip target from the C3 envs above (dual-core
; Xtensa LX7 vs single-core RISC-V) -- CLAUDE.md's "380KB RAM hard ceiling" and
; "NO PSRAM" hardware constraints describe the C3 envs only; these S3 boards
; have 8MB PSRAM. EINK_DISPLAY_SINGLE_BUFFER_MODE stays on for these too,
; matching freeink-sdk's own reference platformio.sample.ini -- there is no
; known reason yet to spend the PSRAM headroom on double buffering.
;
; Both devices' BoardConfig.h profiles include fields explicitly marked
; PENDING hardware validation upstream (panel orientation, SD bus sharing,
; touch swap/flip, exact frontlight GPIO/freq -- see
; freeink-sdk/docs/xteink-x4pro-support.md and the STICKY BoardProfile comment
; in freeink-sdk/libs/hardware/BoardConfig/include/BoardConfig.h). Neither
; target has been booted on real hardware from this repo. Compiles cleanly as
; of this commit; nothing beyond that is verified.
;
; Neither device has physical Back/Left/Right buttons -- both rely on touch
; (GT911) for navigation beyond Up/Down paging and the power button. Touch
; input is not yet wired into this firmware's InputManager/HalGPIO layer, so
; these builds are not usable as a reading device yet -- that's the separate
; touch-input follow-up.
; ============================================================================

; --- Sticky (Seeed reTerminal Sticky) -- ESP32-S3, SSD1677 800x480 + GT911 ----
[env:sticky]
extends = base
board = esp32-s3-devkitc1-n16r8
board_build.mcu = esp32s3
build_flags =
  ${base.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}-sticky-dev\"
  -DFREEINK_DEVICE_STICKY=1

; --- Xteink X4 Pro -- ESP32-S3 (8MB PSRAM), SSD1677 800x480 + GT911 + frontlight
[env:x4pro]
extends = base
board = esp32-s3-devkitc1-n16r8
board_build.mcu = esp32s3
build_flags =
  ${base.build_flags}
  -DCROSSPOINT_VERSION=\"${crosspoint.version}-x4pro-dev\"
  -DBOARD_HAS_PSRAM
  -DFREEINK_DEVICE_X4PRO=1
  ; freeink-sdk/docs/xteink-x4pro-support.md is explicit that X4 Pro's SD card
  ; is native SDMMC and silent to SPI-mode CMD0, and that "the consumer build
  ; must define USE_BLOCK_DEVICE_INTERFACE=1 (the x4pro env does)" -- but as of
  ; the freeink-sdk commit this repo currently pins, platformio.sample.ini's own
  ; [env:x4pro] does NOT actually set this flag (only [env:delink], the other
  ; SDMMC device, does). Following the doc's explicit statement over the
  ; apparently-stale sample here; SD access on X4 Pro would otherwise silently
  ; fail to mount per the doc's own explanation.
  -DUSE_BLOCK_DEVICE_INTERFACE=1

; Desktop simulator (github.com/crosspoint-reader/crosspoint-simulator) — renders the
; e-ink display in an SDL2 window on the host, no device required. Requires `brew
; install sdl2` on macOS. Run with: pio run -e simulator -t run_simulator
[env:simulator]
platform = native
build_src_filter =
  +<*>
  ; These get host-backed equivalents/no-op shims from the simulator library.
  -<network/CrossPointWebServer.cpp>
  -<network/FirmwareFlasher.cpp>
  -<network/OtaBootSwitch.cpp>
  -<network/OtaUpdater.cpp>
  -<network/WebDAVHandler.cpp>
  -<platform/skip_efuse_blk_check.c>
build_flags =
  -std=gnu++2a
  !sdl2-config --cflags --libs
  -Wno-c++11-narrowing
  -DSIMULATOR
  -DCROSSPOINT_VERSION=\"dev-simulator\"
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=2
  -DEINK_DISPLAY_SINGLE_BUFFER_MODE=1
  -DMINIZ_NO_ZLIB_COMPATIBLE_NAMES=1
  -DXML_GE=0
  -DXML_CONTEXT_BYTES=1024
  -DUSE_UTF8_LONG_NAMES=1
  -DPNG_MAX_BUFFERED_PIXELS=16416
  -DDISABLE_FS_H_WARNING=1
  -DDESTRUCTOR_CLOSES_FILE=1
  -Isrc
lib_ldf_mode = deep+
lib_ignore = hal, PNGdec, JPEGDEC
extra_scripts =
  pre:scripts/gen_i18n.py
  pre:scripts/git_branch.py
  pre:scripts/build_html.py
lib_deps =
  simulator=https://github.com/crosspoint-reader/crosspoint-simulator
  bblanchon/ArduinoJson @ 7.4.2
  ricmoo/QRCode @ ^0.0.1
  links2004/WebSockets @ 2.7.3

; Same simulator, but reports X3 (792px, narrower) to HalGPIO::deviceIsX3() instead
; of the [env:simulator] default of X4 (800px) -- needed to catch X3-only layout bugs
; (e.g. kashida/justification math that happens to work at 800px but not 792px)
; that [env:simulator] alone can never exercise, since real X3 hardware wasn't
; available to test directly. Duplicated rather than `extends`ing [env:simulator]
; directly (extends only reliably pulls in shared [base]-style sections in this
; PlatformIO version, not another concrete [env:*]'s extra_scripts/build_src_filter --
; extending it silently dropped extra_scripts, failing the gen_i18n.py pre-script).
; Run with: pio run -e simulator_x3 -t run_simulator
[env:simulator_x3]
platform = native
build_src_filter =
  +<*>
  ; These get host-backed equivalents/no-op shims from the simulator library.
  -<network/CrossPointWebServer.cpp>
  -<network/FirmwareFlasher.cpp>
  -<network/OtaBootSwitch.cpp>
  -<network/OtaUpdater.cpp>
  -<network/WebDAVHandler.cpp>
  -<platform/skip_efuse_blk_check.c>
build_flags =
  -std=gnu++2a
  !sdl2-config --cflags --libs
  -Wno-c++11-narrowing
  -DSIMULATOR
  -DSIMULATOR_DEVICE_X3
  -DCROSSPOINT_VERSION=\"dev-simulator-x3\"
  -DENABLE_SERIAL_LOG
  -DLOG_LEVEL=2
  -DEINK_DISPLAY_SINGLE_BUFFER_MODE=1
  -DMINIZ_NO_ZLIB_COMPATIBLE_NAMES=1
  -DXML_GE=0
  -DXML_CONTEXT_BYTES=1024
  -DUSE_UTF8_LONG_NAMES=1
  -DPNG_MAX_BUFFERED_PIXELS=16416
  -DDISABLE_FS_H_WARNING=1
  -DDESTRUCTOR_CLOSES_FILE=1
  -Isrc
lib_ldf_mode = deep+
lib_ignore = hal, PNGdec, JPEGDEC
extra_scripts =
  pre:scripts/gen_i18n.py
  pre:scripts/git_branch.py
  pre:scripts/build_html.py
lib_deps =
  simulator=https://github.com/crosspoint-reader/crosspoint-simulator
  bblanchon/ArduinoJson @ 7.4.2
  ricmoo/QRCode @ ^0.0.1
  links2004/WebSockets @ 2.7.3
