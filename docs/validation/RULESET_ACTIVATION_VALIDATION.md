# ruleset-activation validation (disposable)

This file exists only to validate that the new GitHub ruleset
("Develop Branch Protection (Thin-Fork Guard)") is correctly wired on a
live protected `develop` branch -- required status checks, up-to-date
policy, allowed merge methods. Midad-owned, touches no CrossPoint-owned
path. Not meant to be merged -- this PR will be closed without merging
once validation is complete.

Commit 1: initial harmless addition.
